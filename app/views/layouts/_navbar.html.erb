<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">TurboChat</a>
    <ul class="navbar-nav ms-auto">
      <% if current_user %>
          <li class="nav-item d-flex align-items-center">
            <%= link_to "Start Chatting", rooms_path, class: "btn btn-outline-primary ms-2" %>
            <%= link_to "Edit Account", edit_user_registration_path, class: "btn btn-outline-primary ms-2" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "btn btn-outline-primary ms-2" %>
          </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline-primary ms-2" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-outline-secondary ms-2" %>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
